---
import { CONTACT_INFO } from '../../utils/constants';

const currentYear = new Date().getFullYear();
---

<footer class="footer">
	<div class="container">
		<div class="footer-grid">
			<div class="footer-col">
				<h3>Clínica Dental</h3>
				<p>Más de 20 años cuidando sonrisas en Pamplona y Tafalla.</p>
				<img 
					src="/icons/logo-dental-1.png" 
					alt="Logo Clínica Dental Sada Borrás" 
					width="180"
					height="32"
					class="footer-logo"
				/>
			</div>
			
			<div class="footer-col">
				<h3>Pamplona</h3>
				<address>
					<p>{CONTACT_INFO.pamplona.address}</p>
					<p>C.P. {CONTACT_INFO.pamplona.postalCode} - {CONTACT_INFO.pamplona.city}</p>
					<p>Tel: <a href={`tel:+34${CONTACT_INFO.pamplona.phone.replace(/\s/g, '')}`}>{CONTACT_INFO.pamplona.phone}</a></p>
					<p>Móvil: <a href={`tel:+34${CONTACT_INFO.pamplona.mobile.replace(/\s/g, '')}`}>{CONTACT_INFO.pamplona.mobile}</a></p>
					<p>Email: <a href={`mailto:${CONTACT_INFO.pamplona.email}`}>{CONTACT_INFO.pamplona.email}</a></p>
				</address>
			</div>
			
			<div class="footer-col">
				<h3>Tafalla</h3>
				<address>
					<p>{CONTACT_INFO.tafalla.address}</p>
					<p>C.P. {CONTACT_INFO.tafalla.postalCode} - {CONTACT_INFO.tafalla.city}</p>
					<p>Tel: <a href={`tel:+34${CONTACT_INFO.tafalla.phone.replace(/\s/g, '')}`}>{CONTACT_INFO.tafalla.phone}</a></p>
					<p>Email: <a href={`mailto:${CONTACT_INFO.tafalla.email}`}>{CONTACT_INFO.tafalla.email}</a></p>
				</address>
			</div>
			
			<div class="footer-col">
				<h3>Información</h3>
				<ul>
					<li><a href="#servicios">Servicios</a></li>
					<li><a href="#equipo">Equipo humano</a></li>
					<li><a href="#clinicas">Clínicas</a></li>
					<li><a href="/avisos-legales">Avisos legales</a></li>
					<li><a href="/politica-privacidad">Política de privacidad</a></li>
					<li><a href="/politica-cookies">Política de cookies</a></li>
				</ul>
			</div>
		</div>
		
		<div class="footer-bottom">
			<p>&copy; {currentYear} Clínica Dental Sada Borrás. Todos los derechos reservados.</p>
			<p class="footer-tagline">Al servicio de todas sus necesidades dentales en Tafalla y Pamplona.</p>
		</div>
	</div>
</footer>

<style>
	.footer {
		background: var(--color-secondary);
		color: white;
		padding: var(--spacing-lg) 0 var(--spacing-md);
		margin-top: 0;
		position: relative;
		z-index: 10;
	}
	
	.footer-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: var(--spacing-md);
		margin-bottom: var(--spacing-md);
	}
	
	.footer-col h3 {
		color: var(--color-primary);
		font-size: var(--font-size-lg);
		margin-bottom: 1rem;
	}
	
	.footer-col p {
		margin-bottom: 0.5rem;
		line-height: 1.6;
	}
	
	.footer-col address {
		font-style: normal;
	}
	
	.footer-col a {
		color: white;
		transition: color var(--transition);
	}
	
	.footer-col a:hover {
		color: var(--color-primary);
	}
	
	.footer-col ul {
		list-style: none;
	}
	
	.footer-col ul li {
		margin-bottom: 0.5rem;
	}
	
	.footer-logo {
		margin-top: 1rem;
		filter: brightness(0) invert(1);
		opacity: 0.8;
	}
	
	.footer-bottom {
		border-top: 1px solid rgba(255, 255, 255, 0.1);
		padding-top: var(--spacing-md);
		text-align: center;
	}
	
	.footer-bottom p {
		margin-bottom: 0.5rem;
		font-size: var(--font-size-sm);
		opacity: 0.8;
	}
	
	.footer-tagline {
		color: var(--color-primary);
		font-weight: 500;
	}
	
	@media (max-width: 767px) {
		.footer-grid {
			grid-template-columns: 1fr;
			gap: var(--spacing-md);
		}
	}
</style>

