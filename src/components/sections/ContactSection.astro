---
import { CONTACT_INFO } from '../../utils/constants';
import { asset } from '../../utils/assets';
---

<section class="contact section" id="contacto">
	<div class="container">
		<div class="section-header text-center">
			<h2>Contacta con Nosotros</h2>
			<p class="section-subtitle">Visítanos en nuestras clínicas o solicita información</p>
		</div>
		
		<div class="contact-grid-three">
			<!-- Clínica Pamplona -->
			<div class="clinic-contact">
				<h3 class="clinic-name">Pamplona</h3>
				
				<div class="contact-info">
					<div class="info-item">
						<div class="info-icon">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
								<circle cx="12" cy="10" r="3"></circle>
							</svg>
						</div>
						<div class="info-content">
							<p class="info-text">{CONTACT_INFO.pamplona.address}</p>
							<p class="info-text">C.P. {CONTACT_INFO.pamplona.postalCode} - {CONTACT_INFO.pamplona.city}</p>
						</div>
					</div>
					
					<div class="info-item">
						<div class="info-icon">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
							</svg>
						</div>
						<div class="info-content">
							<p class="info-text num"><a href={`tel:+34${CONTACT_INFO.pamplona.phone.replace(/\s/g, '')}`}>{CONTACT_INFO.pamplona.phone}</a></p>
							<p class="info-text num"><a href={`tel:+34${CONTACT_INFO.pamplona.mobile.replace(/\s/g, '')}`}>{CONTACT_INFO.pamplona.mobile}</a></p>
						</div>
					</div>
					
					<div class="info-item">
						<div class="info-icon">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
								<polyline points="22,6 12,13 2,6"></polyline>
							</svg>
						</div>
						<div class="info-content">
							<p class="info-text"><a href={`mailto:${CONTACT_INFO.pamplona.email}`}>{CONTACT_INFO.pamplona.email}</a></p>
						</div>
					</div>
				</div>
				
				<div class="map-container">
					<iframe 
						src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2973.7849305684867!2d-1.6547428!3d42.8160167!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0xd50929c8a7f7d87%3A0x9d4c5f8a8e8e8e8e!2sAvenida%20P%C3%ADo%20XII%2C%208%2C%2031008%20Pamplona%2C%20Navarra!5e0!3m2!1ses!2ses!4v1234567890"
						width="100%"
						height="350"
						style="border:0;"
						allowfullscreen=""
						loading="lazy"
						referrerpolicy="no-referrer-when-downgrade"
					></iframe>
				</div>
			</div>
			
			<!-- Clínica Tafalla -->
			<div class="clinic-contact">
				<h3 class="clinic-name">Tafalla</h3>
				
				<div class="contact-info">
					<div class="info-item">
						<div class="info-icon">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
								<circle cx="12" cy="10" r="3"></circle>
							</svg>
						</div>
						<div class="info-content">
							<p class="info-text">{CONTACT_INFO.tafalla.address}</p>
							<p class="info-text">C.P. {CONTACT_INFO.tafalla.postalCode} - {CONTACT_INFO.tafalla.city}</p>
						</div>
					</div>
					
					<div class="info-item">
						<div class="info-icon">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
							</svg>
						</div>
						<div class="info-content">
							<p class="info-text num"><a href={`tel:+34${CONTACT_INFO.tafalla.phone.replace(/\s/g, '')}`}>{CONTACT_INFO.tafalla.phone}</a></p>
						</div>
					</div>
					
					<div class="info-item">
						<div class="info-icon">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
								<path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
								<polyline points="22,6 12,13 2,6"></polyline>
							</svg>
						</div>
						<div class="info-content">
							<p class="info-text"><a href={`mailto:${CONTACT_INFO.tafalla.email}`}>{CONTACT_INFO.tafalla.email}</a></p>
						</div>
					</div>
				</div>
				
				<div class="map-container">
					<iframe 
						src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2982.3749305684867!2d-1.6727428!3d42.5267167!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0xd50a8c8a7f7d87%3A0x9d4c5f8a8e8e8e8e!2sDiputaci%C3%B3n%20Foral%2C%202%2C%2031300%20Tafalla%2C%20Navarra!5e0!3m2!1ses!2ses!4v1234567890"
						width="100%"
						height="350"
						style="border:0;"
						allowfullscreen=""
						loading="lazy"
						referrerpolicy="no-referrer-when-downgrade"
					></iframe>
				</div>
			</div>
			
			<!-- Formulario de contacto -->
			<div class="form-content">
				<h3 class="form-title">Pide cita, te ayudaremos</h3>
				<p class="form-description">
					Te asesoramos de manera personalizada para ofrecerte el tratamiento más adecuado a tus necesidades, garantizando tu salud, función y estética bucodental.
				</p>

				<form class="contact-form" id="contactForm">
					<div class="form-row">
						<div class="form-group">
							<input 
								type="text" 
								id="nombre" 
								name="nombre" 
								placeholder="Nombre*" 
								required
							/>
						</div>
						<div class="form-group">
							<input 
								type="text" 
								id="apellido" 
								name="apellido" 
								placeholder="Apellido*" 
								required
							/>
						</div>
					</div>

					<div class="form-row">
						<div class="form-group">
							<input 
								type="email" 
								id="email" 
								name="email" 
								placeholder="E-mail*" 
								required
							/>
						</div>
						<div class="form-group">
							<input 
								type="tel" 
								id="telefono" 
								name="telefono" 
								placeholder="Teléfono*" 
								required
							/>
						</div>
					</div>

					<div class="form-group full-width">
						<textarea 
							id="mensaje" 
							name="mensaje" 
							rows="6" 
							placeholder="Mensaje..."
						></textarea>
					</div>

					<div class="form-footer">
						<div class="privacy-check">
							<input 
								type="checkbox" 
								id="privacy" 
								name="privacy" 
								required
							/>
							<label for="privacy">
								Acepto la <a href="#" class="privacy-link">política de privacidad</a>*
							</label>
						</div>
						<button type="submit" class="submit-btn">ENVIAR</button>
					</div>

					<details class="legal-info">
						<summary>Información legal</summary>
						<div class="legal-content">
							<p><strong>Responsable:</strong> Clínica Dental Sada Borrás</p>
							<p><strong>Finalidad:</strong> Responder a su consulta.</p>
							<p><strong>Legitimación:</strong> Consentimiento del interesado.</p>
							<p><strong>Destinatarios:</strong> Los datos quedarán almacenados en nuestro servidor. No se comunicará a terceros.</p>
							<p><strong>Derechos:</strong> Acceso, rectificación, supresión, limitación, portabilidad y olvido de los datos.</p>
						</div>
					</details>
				</form>
			</div>
		</div>
	</div>
</section>

<script>
	const form = document.getElementById('contactForm') as HTMLFormElement;
	
	if (form) {
		form.addEventListener('submit', (e) => {
			e.preventDefault();
			
			// Aquí puedes añadir la lógica para enviar el formulario
			const formData = new FormData(form);
			
			console.log('Formulario enviado:', {
				nombre: formData.get('nombre'),
				apellido: formData.get('apellido'),
				email: formData.get('email'),
				telefono: formData.get('telefono'),
				mensaje: formData.get('mensaje')
			});
			
			// Mostrar mensaje de éxito
			alert('Gracias por tu mensaje. Nos pondremos en contacto contigo pronto.');
			form.reset();
		});
	}
</script>

<style>
	.contact {
		background: white;
		position: relative;
		z-index: 10;
		border-bottom: 1px solid var(--color-divider);
	}
	
	/* Estilos para la información de las clínicas */
	.contact .section-header h2 {
		font-size: 38px;
		color: var(--c-ink);
		font-family: var(--ff-head);
		font-weight: 700;
		line-height: 1.2;
		padding-bottom: 1rem;
		border-bottom: 3px solid var(--c-ink);
		display: inline-block;
	}
	
	.contact .section-subtitle {
		font-size: 16px;
		color: var(--c-gray-700);
	}
	
	.contact-grid-three {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--spacing-md);
		margin-top: var(--spacing-lg);
		align-items: start;
	}
	
	.clinic-contact,
	.form-content {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}
	
	.clinic-name,
	.form-title {
		font-size: 15px;
		color: var(--c-ink);
		font-weight: 700;
		font-family: var(--ff-body);
		text-transform: uppercase;
		letter-spacing: 0.5px;
		margin: 0;
		padding-bottom: 0.75rem;
		border-bottom: 3px solid var(--c-ink);
	}
	
	.contact-info {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}
	
	.info-item {
		display: flex;
		gap: 1rem;
		align-items: flex-start;
	}
	
	.info-icon {
		width: 48px;
		height: 48px;
		background: var(--color-primary-light);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
	}
	
	.info-icon svg {
		color: var(--color-primary);
	}
	
	.info-content {
		flex: 1;
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}
	
	.info-text {
		margin: 0;
		color: var(--c-ink);
		font-size: 14px;
		line-height: 1.5;
	}
	
	.info-text a {
		color: var(--color-primary);
		text-decoration: none;
		transition: var(--transition);
	}
	
	.info-text a:hover {
		color: var(--color-primary-dark);
		text-decoration: underline;
	}
	
	.map-container {
		width: 100%;
		overflow: hidden;
		box-shadow: var(--shadow-md);
	}
	
	.map-container iframe {
		display: block;
	}
	
	/* Estilos para el formulario de contacto */
	.form-description {
		color: var(--c-gray-700);
		font-size: var(--fs-small);
		line-height: 1.5;
		margin: 0;
	}
	
	.contact-form {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}
	
	.form-row {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1rem;
	}
	
	.form-group {
		display: flex;
		flex-direction: column;
	}
	
	.form-group.full-width {
		grid-column: 1 / -1;
	}
	
	.form-group input,
	.form-group textarea {
		padding: 0.7rem 0.9rem;
		border: 1px solid var(--c-gray-200);
		border-radius: var(--rad);
		background: white;
		font-size: var(--fs-body);
		font-family: var(--ff-body);
		color: var(--c-ink);
		transition: var(--transition);
	}
	
	.form-group input:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: var(--c-primary);
		box-shadow: 0 0 0 3px rgba(0, 160, 151, 0.1);
	}
	
	.form-group input::placeholder,
	.form-group textarea::placeholder {
		color: var(--c-gray-500);
	}
	
	.form-group textarea {
		resize: vertical;
		min-height: 120px;
	}
	
	.form-footer {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		margin-top: 0.5rem;
	}
	
	.privacy-check {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		flex: 1;
	}
	
	.privacy-check input[type="checkbox"] {
		width: 18px;
		height: 18px;
		border: 1px solid var(--c-gray-200);
		border-radius: 4px;
		cursor: pointer;
		flex-shrink: 0;
		accent-color: var(--c-primary);
	}
	
	.privacy-check label {
		font-size: var(--fs-small);
		color: var(--c-ink);
		cursor: pointer;
	}
	
	.privacy-link {
		color: var(--c-primary);
		text-decoration: none;
		transition: var(--transition);
	}
	
	.privacy-link:hover {
		text-decoration: underline;
	}
	
	.submit-btn {
		font: 600 1rem/1 var(--ff-body);
		background: var(--c-primary);
		color: white;
		border: 2px solid var(--c-primary);
		border-radius: var(--rad);
		padding: 0.75rem 1.5rem;
		cursor: pointer;
		transition: var(--transition);
		flex-shrink: 0;
		letter-spacing: 0.3px;
	}
	
	.submit-btn:hover {
		background: white;
		color: var(--c-primary);
		border: 2px solid var(--c-primary);
		transform: translateY(-1px);
	}
	
	.submit-btn:active {
		transform: translateY(0);
	}
	
	.legal-info {
		margin-top: 1rem;
		font-size: var(--fs-small);
		color: var(--c-gray-700);
	}
	
	.legal-info summary {
		cursor: pointer;
		color: var(--c-primary);
		font-weight: 600;
		list-style: none;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}
	
	.legal-info summary::-webkit-details-marker {
		display: none;
	}
	
	.legal-info summary::before {
		content: '▼';
		font-size: 10px;
		transition: transform 0.3s;
	}
	
	.legal-info[open] summary::before {
		transform: rotate(-180deg);
	}
	
	.legal-content {
		margin-top: 1rem;
		padding: 1rem;
		background: white;
		border-radius: var(--rad);
		border: 1px solid var(--c-gray-200);
	}
	
	.legal-content p {
		margin: 0.5rem 0;
		font-size: var(--fs-small);
		line-height: 1.5;
	}
	
	/* Responsive */
	@media (max-width: 1200px) {
		.contact-grid-three {
			grid-template-columns: 1fr;
			gap: var(--spacing-lg);
		}
		
		.form-description {
			font-size: var(--fs-body);
		}
	}
	
	@media (max-width: 767px) {
		.map-container iframe {
			height: 300px !important;
		}
		
		.form-row {
			grid-template-columns: 1fr;
		}
		
		.form-footer {
			flex-direction: column;
			align-items: stretch;
		}
		
		.submit-btn {
			width: 100%;
		}
	}
</style>
