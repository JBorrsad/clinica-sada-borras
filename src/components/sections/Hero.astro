---
import { asset } from '../../utils/assets';
// Hero section con contenido optimizado para SEO
---

<section class="hero" id="inicio">
	<div class="hero-bg">
		<img 
			src={asset("/img/IMG_7699-Large-1024x768.jpeg")}
			alt="Interior moderno de Clínica Dental Sada Borrás en Pamplona"
			loading="eager"
		/>
	</div>
	<div class="hero-overlay"></div>
	<div class="container hero-content">
		<div class="hero-left">
			<h1>Tu Clínica Dental de Confianza en Pamplona y Tafalla</h1>
		</div>
		<div class="hero-right">
			<p class="hero-description">Llevamos más de 20 años cuidando sonrisas en Navarra. Cuidamos cada aspecto de tu experiencia dental con un trato cercano, profesional y adaptado a ti en unas instalaciones modernas equipadas con tecnología avanzada.</p>
			<div class="hero-cta">
				<a href="#contacto" class="btn btn-primary">¡PIDE CITA!</a>
			</div>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		min-height: 600px;
		display: flex;
		align-items: center;
		overflow: hidden;
		border-bottom: 1px solid var(--color-divider);
	}
	
	.hero-bg {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: 0;
	}
	
	.hero-bg img {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}
	
	.hero-overlay {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: linear-gradient(135deg, rgba(0, 191, 166, 0.85) 0%, rgba(26, 26, 46, 0.85) 100%);
		z-index: 1;
	}
	
	.hero-content {
		position: relative;
		z-index: 2;
		padding: var(--spacing-xl) 0;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: start;
	}
	
	.hero-right {
		padding-top: 7rem;
		color: white;
	}
	
	.hero-left {
		color: white;
	}
	
	.hero h1 {
		font-size: 40px;
		color: white;
		margin-bottom: 1.5rem;
		line-height: 1.2;
		font-family: var(--ff-head);
		font-weight: 700;
	}
	
	.hero-description {
		font-size: 19px;
		margin-bottom: 2rem;
		opacity: 0.95;
		line-height: 1.6;
		color: white;
	}
	
	.hero-cta {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		margin-top: 1.5rem;
	}
	
	.btn {
		padding: 1rem 2rem;
		border-radius: var(--border-radius);
		font-weight: 600;
		font-size: 15px;
		transition: var(--transition);
		display: inline-block;
		text-align: center;
		font-family: var(--ff-body);
	}
	
	.btn-primary {
		background: var(--color-primary);
		color: white;
	}
	
	.btn-primary:hover {
		background: var(--color-primary-dark);
		transform: translateY(-3px);
		box-shadow: var(--shadow-lg);
	}
	
	
	@media (max-width: 767px) {
		.hero {
			min-height: 500px;
		}
		
		.hero-content {
			grid-template-columns: 1fr;
			gap: 2rem;
		}
		
		.hero h1 {
			font-size: 32px;
		}
		
		.hero-description {
			font-size: 17px;
		}
		
		.hero-right {
			padding-top: 0;
		}
		
		.btn {
			width: 100%;
		}
	}
</style>

