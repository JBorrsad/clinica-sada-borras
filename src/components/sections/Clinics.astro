---
import { asset } from '../../utils/assets';
---

<section class="clinics section" id="clinicas">
	<div class="container">
		<!-- Título separado arriba -->
		<h2 class="clinics-title">Espacios pensados para ti</h2>
		
		<div class="clinics-content">
			<!-- Texto a la izquierda -->
			<div class="clinics-text">
				<p class="section-subtitle">Con más de 40 años de experiencia y un equipo en formación continua, trabajamos con tecnología de última generación y técnicas actuales en unas instalaciones cuidadas, pensadas para que te sientas en buenas manos y vivas una experiencia cómoda y sencilla, como en casa.</p>
				
				<!-- Botón debajo del texto -->
				<div class="clinics-cta">
					<a href={`${import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : import.meta.env.BASE_URL + '/'}instalaciones`} class="btn btn-primary">Descubre nuestras clínicas</a>
				</div>
			</div>
			
			<!-- Imagen a la derecha -->
			<div class="clinics-image">
				<img 
					src={asset('img/instalaciones/top2.jpeg')}
					alt="Clínica Dental Sada Borrás"
					width="600"
					height="400"
					loading="lazy"
				/>
			</div>
		</div>
	</div>
</section>

<style>
	.clinics {
		background: white;
		position: relative;
		z-index: 10;
	}
	
	.clinics .container {
		display: flex;
		flex-direction: column;
		align-items: center;
	}
	
	.clinics-title {
		font-size: 38px;
		color: var(--c-ink);
		font-family: var(--ff-head);
		font-weight: 700;
		line-height: 1.2;
		text-align: center;
		margin-bottom: 3rem;
		padding-bottom: 1rem;
		border-bottom: 3px solid var(--c-ink);
		display: inline-block;
		width: auto;
	}
	
	.clinics-content {
		display: grid;
		grid-template-columns: 1fr 1.2fr;
		gap: var(--spacing-lg);
		align-items: start;
		width: 100%;
	}
	
	.clinics-text {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-lg);
		margin-top: 2rem;
	}
	
	.section-subtitle {
		font-size: var(--fs-body);
		color: var(--c-gray-700);
		line-height: 1.6;
		margin: 0;
	}
	
	.clinics-image {
		overflow: hidden;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08), 0 2px 8px rgba(0, 0, 0, 0.04);
		justify-self: end;
		width: 100%;
		height: 300px;
	}
	
	.clinics-image img {
		width: 100%;
		height: 100%;
		display: block;
		object-fit: cover;
		object-position: center;
	}
	
	.clinics-cta {
		margin-top: 0;
	}
	
	.btn {
		padding: 1rem 2rem;
		border-radius: var(--border-radius);
		font-weight: 600;
		font-size: var(--font-size-base);
		transition: var(--transition);
		display: inline-block;
		text-align: center;
		text-decoration: none;
		border: none;
		cursor: pointer;
	}
	
	.btn-primary {
		background: var(--color-primary);
		color: white;
	}
	
	.btn-primary:hover {
		background: var(--color-primary-dark);
		transform: translateY(-2px);
		box-shadow: var(--shadow-md);
	}
	
	@media (max-width: 768px) {
		.clinics-title {
			font-size: 32px;
			margin-bottom: 2rem;
		}
		
		.clinics-content {
			grid-template-columns: 1fr;
			gap: var(--spacing-lg);
		}
		
		.clinics-text {
			margin-top: 1rem;
		}
		
		.clinics-image {
			justify-self: center;
			height: 250px;
		}
	}
</style>
